import { PersianDateField } from "@marzneshin/common/components";
import { FC } from "react";

export const ExpireDateField: FC = () => {
    return <PersianDateField name="expire_date" label="page.users.expire_date" />;
};
