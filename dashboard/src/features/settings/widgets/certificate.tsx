import {
    MiniWidget
} from "@marzneshin/components";
import {
    CertificateButton
} from "@marzneshin/features/settings";

export const CertificateWidget = () => {
    return (
        <MiniWidget
            title="Ceritifcate"
        >
            <CertificateButton />
        </MiniWidget>
    )
}
